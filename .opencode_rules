# 前端開發標準作業程序 (SOP)

每當使用者要求修改前端相關代碼（位於 `src/` 或相關目錄）時，請嚴格執行以下步驟：

1. **實作改動**：根據 OpenSpec 的定義修改代碼。
2. **自動驗證**：修改完成後，必須主動在終端機執行 `npm run build`。
3. **錯誤處理**：
   - 若 Build 失敗：分析 terminal 報錯，自動修正代碼，並重複執行 Step 2。
   - 若 Build 成功：檢查是否有任何 Warning，若無則進入下一步。
4. **交付確認**：只有在 Build 成功後，才能回覆「已完成修改並通過編譯驗證」。

# 後端開發與環境驗證規範 (Docker SOP)

每當涉及後端代碼（如 Python, Go, Node.js, PHP,C# 等）、資料庫結構 (SQL)、或 Docker 相關配置 (Dockerfile, docker-compose.yml) 的修改時，請嚴格遵守以下流程：

1. **實作改動**：根據 OpenSpec 的規格進行代碼開發或配置修改。
2. **自動驗證環境**：
   - 修改完成後，必須主動在終端機執行：`docker-compose up -d --build`。
   - 執行後，請使用 `docker-compose ps` 檢查所有服務的狀態是否為 `Up` (running)。
3. **錯誤處理循環**：
   - **若啟動失敗或崩潰 (Exit 1)**：請主動執行 `docker-compose logs [服務名稱]` 分析報錯原因。
   - 根據日誌修正代碼或 Dockerfile，並重新執行 Step 2，直到服務穩定運行。
4. **交付定義 (DoD)**：
   - 只有在容器成功啟動且沒有持續重啟的情況下，才允許回報完成。
   - 回報時，請簡要說明：「環境已重新編譯並成功啟動，目前容器狀態正常」。

# 限制與安全
- 除非使用者要求，否則請勿在執行 `up` 前刪除數據卷 (Volumes)，以免造成開發資料遺失。